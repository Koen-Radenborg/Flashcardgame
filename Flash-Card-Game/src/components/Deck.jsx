const Deck = ({title, shortDesc, bgColor, borderColor}) => {
    return ( 
        <>
        <div>
            <h2>{title}</h2>
            <p>{shortDesc}</p>
            
        </div>
        </>
     );
}
 
export default Deck;